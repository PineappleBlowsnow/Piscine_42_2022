#include <stdio.h>

unsigned int ft_strlcat(char* dest, char *src, unsigned int size);
int main()
{

   /* for (int size = 0; size < 15; size++)
    {
        char dest[15] = "dst\0";
        char src[] = "src\0";
        printf("%d:%s;%s;", size, dest, src);
        int l = ft_strlcat(dest, src, size);
        printf("%s;%d;\n", dest, l);
    }
    */
   
printf("%d\n",-1<<31);
for(int n=-10;n<34;n++)
   printf("%d\n",1<<n);
}